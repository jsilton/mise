# Mint tzatziki: one editable refinement candidate

Candidate: `/tmp/mise-mint-watercolor-refinement.svg` (960 × 640).
Editable authoring source: `/tmp/mise-mint-watercolor-refinement.mjs`.
Browser inspection: `/tmp/mise-mint-watercolor-refinement-browser.png`.
Preview document: `/tmp/mise-mint-watercolor-preview.html`.

The candidate replaces the existing study's repeated, interlocking yogurt ribbons with sparse shallow spoon marks and large uninterrupted white areas. The pale vessel uses interrupted contour fragments and locally disappearing washes; no complete ellipse is stroked. Uneven groups of cucumber dice and chopped mint provide the selective detail. A few darker mint fragments remain crisp against the broader diluted marks. The muted olive-oil suggestion stays mixed into the sauce, without a drizzle or pool. No dill, lemon, whole mint sprig, pepper or other garnish is added.

Seven named editable layers separate optional paper, shadow, pale bowl, yogurt, cucumber, mint and oil. The default paper layer is hidden for transparent placement. All forms are original vector paths and SVG filter effects; XML inspection found 158 paths, no embedded image and no script. The generator writes only this isolated candidate and can have its output path changed for a later integration decision.

I read `docs/VISUAL_IDENTITY.md` and the existing `src/art/render-mint-study.mjs`, and inspected the earlier mint browser preview. The direction follows the documented reference characteristics: open neutral paper, selective detail and lost edges. The original Desktop reference files were not available in the current Desktop listing. The similarly named file recovered under `/tmp/mise-photo-framing/` is a screenshot of a photographic-framing experiment, not a usable watercolor reference; it was not treated as evidence of a style match.

Isolated headless Chrome rendered the final SVG at its 960 px natural width and 320 px on three near-neutral preview backgrounds. I visually inspected the final screenshot. There is no clipping or missing artwork in those views. This is a standalone preview, not verification in the live site or its exact palette tokens. The screenshot is browser-rendered inspection evidence, not a separately authored raster asset.

The yogurt has less sculpted relief than the earlier study, but the bowl remains a shallow oval vessel and the small ingredients are still delicate at thumbnail size. Filter granulation approximates pigment; it does not reproduce every property of hand-painted watercolor. The pale vessel is not asserted to be a specific material. No actual-dish photograph was used, and the drawing does not establish portion accuracy or a kitchen result.

No repository files were changed. Hold this candidate for root/user visual evaluation; it is not approved final recipe artwork and should not automatically replace the current study or paintings.
